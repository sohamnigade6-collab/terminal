# Local development (Vite proxy handles /api/* → localhost:3001)
# No VITE_API_URL needed in dev

# Uncomment and set this when running frontend locally against a deployed backend:
# VITE_API_URL=https://blossom-backend.vercel.app
